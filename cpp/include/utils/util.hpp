#include <iostream>

#ifndef UTIL_H
#define UTIL_H

#define DEBUG(msg) std::cerr << "DEBUG: " << (msg) << '\n';
#define LOG(msg) std::cerr << "LOG: " << (msg) << '\n';
#define TEST(msg) std::cerr << "TEST: " << (msg) << '\n'

#endif // UTIL_H